* text=auto

# Scripts executed inside Linux containers or CI must never receive CRLF.
*.sh text eol=lf
mvnw text eol=lf

# Native Windows entry points keep their platform line endings.
*.cmd text eol=crlf
*.ps1 text eol=crlf
